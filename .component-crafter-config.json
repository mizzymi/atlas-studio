{
  "showExtraDetails": false,
  "specialProperty": "|PR0PS|",
  "commands": [
    {
      "name": "component",
      "description": "with this command you can create a component in a Next.js application.",
      "startingPath": ["atlasstudio-frontend", "src", "components"],
      "scaffold": [
        {
          "type": "folder",
          "name": "|PR0PS|",
          "content": [
            {
              "type": "file",
              "name": "|PR0PS|.tsx",
              "content": "import { type FC } from 'react';\n\n/**\n * **PROPERTIES OF APP COMPONENT:**\n * \n * This interface currently have 0 properties but if you add any property \n * you must document it and type it correctly for the usability of the component.\n * \n */\ninterface |PR0PS|Props {\n\n}\n\n/**\n * **DESCRIPTION:**\n * \n * Write something here about the |PR0PS| component.\n * \n * **EXAMPLE OF USE:**\n * @example\n * return (\n *   <|PR0PS|/>\n * )\n */\nexport const |PR0PS|: FC<|PR0PS|Props> = ({ }) => {\n\n  return (\n    <div data-testid='|PR0PS|-Component'>\n      Entry Point for |PR0PS| component. Good Luck!\n    </div>\n  )\n}\n"
            }
          ]
        }
      ]
    },
    {
      "name": "hook",
      "description": "with this command you can create a custom hook in a Next.js application.",
      "startingPath": ["atlasstudio-frontend", "src", "hooks"],
      "scaffold": [
        {
          "type": "folder",
          "name": "use|PR0PS|",
          "content": [
            {
              "type": "file",
              "name": "use|PR0PS|.ts",
              "content": "/**\n * **DESCRIPTION:**\n * \n * With this interface we can represent the return of the\n * custom hook of the use|PR0PS|, that this custom\n * hook will return only one property that an object of this\n * type.\n */\ninterface Use|PR0PS|Return {\n\n}\n\n/** * **DESCRIPTION:**\n * \n * This interface currently have 0 properties but if you add any property\n * you must document it and type it correctly for the usability of the custom hook.\n */\ninterface Use|PR0PS|Props {\n\n}\n\n/**\n * **DESCRIPTION:**\n * \n * You need to write a description of what the use|PR0PS| do on here.\n * \n * **RETURNS:**\n * \n * @returns returns an object with the properties of: \n * * need to write it\n * \n * **EXAMPLE OF USE:**\n * \n * @example\n * const { } = use|PR0PS|({}); \n */\nexport const use|PR0PS| = ({ }: Use|PR0PS|Props): Use|PR0PS|Return => {\n\n    return {\n\n    }\n\n}\n"
            }
          ]
        }
      ]
    },
    {
      "name": "ch",
      "description": "with this command you can create a component with its hook in a Next.js application.",
      "startingPath": ["atlasstudio-frontend", "src", "components"],
      "scaffold": [
        {
          "type": "folder",
          "name": "|PR0PS|",
          "content": [
            {
              "type": "file",
              "name": "|PR0PS|.tsx",
              "content": "'use client';\n\nimport { use|PR0PS| } from '@/hooks/use|PR0PS|/use|PR0PS|';\nimport { type FC } from 'react';\n\n/**\n * **PROPERTIES OF APP COMPONENT:**\n * \n * This interface currently have 0 properties but if you add any property \n * you must document it and type it correctly for the usability of the component.\n * \n */\ninterface |PR0PS|Props {\n\n}\n\n/**\n * **DESCRIPTION:**\n * \n * Write something here about the |PR0PS| component.\n * \n * **EXAMPLE OF USE:**\n * @example\n * return (\n *   <|PR0PS|/>\n * )\n */\nexport const |PR0PS|: FC<|PR0PS|Props> = ({ }) => {\n\n  const { } = use|PR0PS|({});\n\n  return (\n    <div data-testid='|PR0PS|-Component'>\n      Entry Point for |PR0PS| component. Good Luck!\n    </div>\n  )\n}\n"
            }
          ]
        }
      ],
      "commandsAfter": ["yarn craft hook |PR0PS|"]
    },
    {
      "name": "method",
      "description": "with this command you can create a method in a Next.js application.",
      "startingPath": ["atlasstudio-frontend", "src", "methods"],
      "scaffold": [
        {
          "type": "folder",
          "name": "|PR0PS|",
          "content": [
            {
              "type": "file",
              "name": "|PR0PS|.ts",
              "content": "/**\n * **DESCRIPTION:**\n * \n * With this interface we can define the properties that |PR0PS|\n * method need but for now it need no properties.\n */\ninterface |PR0PS|Props {\n\n}\n\n/**\n * **DESCRIPTION:**\n * \n * You need to write a description of what the |PR0PS| do on here.\n * \n * ** PARAMETERS:**\n * \n * Right now you only need to pass an empty object.\n * \n * **EXAMPLE TO USE:**\n * \n * @example\n * |PR0PS|({});\n */\nexport const |PR0PS| = ({}: |PR0PS|Props) => {\n\n}\n"
            }
          ]
        }
      ]
    },
    {
      "name": "reducer",
      "description": "with this command you can create a reducer in a Next.js application.",
      "startingPath": ["atlasstudio-frontend", "src", "store", "reducers"],
      "scaffold": [
        {
          "type": "folder",
          "name": "|PR0PS|",
          "content": [
            {
              "type": "file",
              "name": "|PR0PS|Reducer.ts",
              "content": "import type { AppAction } from '../../../types/AppAction';\n\n/**\n * **DESCRIPTION:**\n * \n * This interface defines the state shape for the `|PR0PS|` slice\n * of the global AppStore.\n * \n * Right now it has 0 properties, but if you add any, you must:\n * - Document what the property represents.\n * - Type it correctly for the usability of the slice.\n */\nexport interface |PR0PS|State {\n\n}\n\n/**\n * **DESCRIPTION:**\n * \n * Initial state for the `|PR0PS|` slice.\n * \n * If you add new properties to `|PR0PS|State`, remember to:\n * - Initialize them here with a safe default value.\n */\nexport const |PR0PS|InitialState: |PR0PS|State = {\n\n};\n\n/**\n * **DESCRIPTION:**\n * \n * Pure reducer function for the `|PR0PS|` slice. It receives the\n * current `|PR0PS|` state and a global `AppAction`, and returns\n * the next state.\n * \n * **PARAMETERS:**\n * \n * @param state Current `|PR0PS|` state.\n * @param action Global action describing how the state should change.\n * \n * **EXAMPLE OF USE (inside the AppStore root reducer):**\n * \n * @example\n * const rootReducer = (state: AppState, action: AppAction): AppState => ({\n *   ...state,\n *   // Replace `yourSliceKey` with the key you use on AppState:\n *   yourSliceKey: |PR0PS|Reducer(state.yourSliceKey, action),\n * });\n */\nexport const |PR0PS|Reducer = (\n  state: |PR0PS|State,\n  action: AppAction\n): |PR0PS|State => {\n  switch (action.type) {\n    // Example:\n    // case '|PR0PS|/SET_SOMETHING':\n    //   return {\n    //     ...state,\n    //     something: action.payload,\n    //   };\n\n    default:\n      return state;\n  }\n};\n"
            },
            {
              "type": "file",
              "name": "index.ts",
              "content": "export * from './|PR0PS|Reducer.ts'"
            }
          ]
        }
      ],
      "commandsAfter": ["node tools/add-reducer-to-index.mjs |PR0PS|"]
    },
    {
      "name": "page",
      "description": "with this command you can create a Next.js page in the app router.",
      "startingPath": ["atlasstudio-frontend", "src", "app"],
      "scaffold": [
        {
          "type": "folder",
          "name": "|PR0PS|",
          "content": [
            {
              "type": "file",
              "name": "page.tsx",
              "content": "import { type FC } from 'react';\n\n/**\n * **DESCRIPTION:**\n * \n * Write something here about the |PR0PS| page.\n * \n * This file represents the route: /|PR0PS|\n * \n * **EXAMPLE OF USE:**\n * Just navigate to /|PR0PS| in your browser.\n */\nconst |PR0PS|Page: FC = () => {\n  return (\n    <main data-testid='|PR0PS|-Page'>\n      Entry Point for |PR0PS| page. Good Luck!\n    </main>\n  );\n};\n\nexport default |PR0PS|Page;\n"
            }
          ]
        }
      ]
    }
  ]
}
